import {Box, Heading} from "@chakra-ui/react";
import React from "react";

const Student = () => (
    <Box>
        <Heading>Student Dashboard</Heading>
        {/* Components to display forms created by admin */}
    </Box>
);

export  default Student
